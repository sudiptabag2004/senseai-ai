version: '3'
services:
  app:
    image: dalmiamanhv/sensai-ai:production
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8001:8001"
    environment:
      - ENV=production
  
  demo:
    image: dalmiamanhv/sensai-ai:production
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8001:8001"
    environment:
      - ENV=production
